stages:
  - build
  - test

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - ./dist
  only:
    - master


test:
  stage: test
  script:
    - npm run lint
  only:
    - master
